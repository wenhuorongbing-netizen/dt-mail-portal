# VS Code Workspaces

The preferred workspace directory is:

```text
D:\【指挥中心】\节操都市\Workspaces
```

Open the current project with:

```text
D:\【指挥中心】\节操都市\Workspaces\Open-DTMailPortal.cmd
```

The old Control Tower workspace should not be reused. Use project-specific `.code-workspace` files so active projects can be opened without hunting through VS Code history.
