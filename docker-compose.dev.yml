services:
  postgres:
    image: postgres:16-alpine
    container_name: dt-mail-postgres
    environment:
      POSTGRES_DB: dt_mail_portal
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
